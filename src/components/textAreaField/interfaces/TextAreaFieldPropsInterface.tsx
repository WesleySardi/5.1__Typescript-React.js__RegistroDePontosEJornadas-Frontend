export interface ITextAreaFieldProps {
  label: string;
  placeholder?: string;
  register: any;
  name: string;
  error?: any;
  classNameDiv: string;
  classNameTextArea: string;
  maxLength?: number;
}
